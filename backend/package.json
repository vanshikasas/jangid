{
  "name": "skja-api-rust",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "cargo run",
    "build": "cargo build --release",
    "start": "cargo run --release",
    "test": "cargo test"
  }
}
